# ===========================================
# 환경변수 설정 파일 예시
# ===========================================
# 이 파일을 복사하여 .env.local 파일을 생성하세요:
#   cp .env.example .env.local (Linux/Mac)
#   copy .env.example .env.local (Windows)
#
# 그 후 실제 값으로 교체하세요.
# ===========================================

# -----------------------------------------
# Supabase 설정
# -----------------------------------------
# Supabase 프로젝트 URL (Supabase 대시보드에서 확인)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Publishable Key (브라우저에서 사용 가능한 공개 키)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# -----------------------------------------
# Database 설정 (Prisma용)
# -----------------------------------------
# Connection Pooling URL (일반 쿼리용 - 포트 6543)
# 비밀번호의 특수문자는 URL 인코딩 필요 (@ → %40, ` → %60)
DATABASE_URL="postgresql://postgres.your-project-id:your_password_here@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct Connection URL (마이그레이션용 - 포트 5432)
DIRECT_URL="postgresql://postgres.your-project-id:your_password_here@aws-1-ap-south-1.pooler.supabase.com:5432/postgres"

# -----------------------------------------
# Slack 설정 (선택사항)
# -----------------------------------------
# Slack Webhook URL (Task 완료 알림용)
# Slack App 설정에서 Incoming Webhook URL을 생성하세요
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
